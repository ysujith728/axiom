{
  "name": "@axiom/config",
  "version": "0.1.0",
  "private": true,
  "description": "Dynamic hardware detection and resource budgeting for AXIOM",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@axiom/shared": "*"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist"
  }
}
