# AXIOM Vision & Windows UI Automation Worker

This worker provides screen understanding and OCR capabilities for AXIOM.

## Capabilities
- Windows UI Automation tree inspection via `pywinauto` / `uiautomation`.
- Fast local OCR via `easyocr` or `pytesseract`.
- Screenshot visual verification and UI bounding box detection.
